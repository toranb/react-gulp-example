A simple react app using gulp for the client side build step


To run the react app locally

    npm install
    gulp

    python -m SimpleHTTPServer 3000

    visit http://localhost:3000 to see the running app


To run the react tests

    npm install
    gulp test
